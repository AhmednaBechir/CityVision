import React, {
  useEffect,
  useState
} from 'react'

import { useStore } from '../store'
import { fetchSchedule } from '../api'
import axios from 'axios'

export default function VoiPanel() {
    return (<div>VOI Panel</div>)
}